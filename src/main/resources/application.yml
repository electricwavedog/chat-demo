spring:
    datasource:
        url: jdbc:mysql://localhost:3306/chat?characterEncoding=utf-8
        username: root
        password: 123456
        driver-class-name: com.mysql.jdbc.Driver
    redis:
        database: 0
        host: localhost
        port: 6379
        password:
        timeout: 100
        jedis:
          pool:
            max-active: 8
            max-wait: -1
            max-idle: 8
            min-idle: 0
    jpa:
        hibernate:
           ddl-auto: update
        show-sql: true
        generate-ddl: true
    thymeleaf:
      cache: false
mybatis:
    mapper-locations: classpath:mappers/*.xml
    typeAliasesPackage: com.example.test.model